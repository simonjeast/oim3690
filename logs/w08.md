# Learning Log - Week 8 

**Topic:** JavaScript

# Question:
- How does adding the controls attribute to <audio> and <video> change what the user can do on the page?

## What I learned
- JavaScript can change page content after it loads
- `let` is used for variables that can change
- `const` is used for values that should stay the same
- Functions help reuse code for repeated actions
- `if` statements let code make simple decisions

## What I did today
- Added simple JavaScript to a page and checked it in the browser console
- Practiced writing variables and a basic function
- Used `console.log()` to test if my code was running

## One thing I'm still unsure about
- When to use `==` vs `===` in JavaScript

## Question for next class
- How does adding the `controls` attribute to `<audio>` and `<video>` change what the user can do on the page?