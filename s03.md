* **What does `<!doctype html>` even do?** Why is it needed at the very top, and what happens if I delete it?
* **What’s the difference between `<head>` and `<body>`?** Why is some stuff “invisible” up top?
* **Why are there so many `<meta>` tags?** Especially the viewport one—what does it change on phones?
* **What does `id="projects"` mean?** How does clicking “Projects” in the nav jump me to that section?
* **Why are there tons of `class="..."` names?** How do classes connect to the CSS and change how things look?
* **What is `:root` in the CSS?** And why do we define colors like `--accent` instead of just using a color directly?
* **What is “responsive” about this?** How does it rearrange itself on smaller screens (like the `@media` parts)?
* **What’s the difference between `href="#contact"` and `href="resume.pdf"`?** One scrolls and one opens a file—why?
* **Why do links sometimes have `target="_blank"` and `rel="noopener"`?** What do those do, and are they required?
* **What is the `<script>` at the bottom doing?** How does it automatically set the year, and is JavaScript always needed?
